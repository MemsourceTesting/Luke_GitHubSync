phrase:
  project_id: 

  push:
    sources:
      - file: ./en.json
        params:
          file_format: simple_json
          locale_id: en

  pull:
    targets:
      - file: ./spaces.properties
        params:
          locale_id: en
          file_format: properties 
          format_options:
            omit_separator_space: true
